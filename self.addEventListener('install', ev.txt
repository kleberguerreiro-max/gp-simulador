self.addEventListener('install', event => {
event.waitUntil(
caches.open('gp-simulador').then(cache => {
return cache.addAll([
'index.html',
'style.css',
'app.js',
'logo.png'
]);
})
);
});
